# Backend API base URL.
# Leave BLANK to use the relative "/api" prefix, which the Vite dev server
# proxies to the backend (same origin, no CORS). Set a full origin
# (e.g. https://api.example.com) only when deployed separately from the backend.
VITE_API_BASE_URL=
